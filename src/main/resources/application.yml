server:
  port: 8085

spring:
  application:
    name: partner-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://partner-db:5432/partner_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  open-in-view: false

jwt:
  secret: ${JWT_SECRET:intellcap_secret_key_2024_very_long_string}
  expiration: 86400000